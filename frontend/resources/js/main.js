import { createApp } from 'vue'
import App from '@/pages/VehicleManagerPage.vue'
import '@/app.css'

// vue-select css (library css, not custom project css)
import 'vue-select/dist/vue-select.css'

createApp(App).mount('#app')